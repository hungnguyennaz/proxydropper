sudo echo "Use netfilter-persistent to save those permanently" && sudo apt -y install ipset && sudo apt -y install curl && sudo curl -o blacklist.txt https://raw.githubusercontent.com/hungnguyennaz/proxydropper/main/blacklist.txt && sudo ipset flush blacklist ; sudo iptables -D INPUT -m set --match-set blacklist src -j DROP ; sudo ipset destroy blacklist ; sudo ipset restore -! < blacklist.txt ; sudo rm blacklist.txt && sudo iptables -I INPUT -m set --match-set blacklist src -j DROP && sudo echo "Job done."
